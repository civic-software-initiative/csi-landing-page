---
import { Image } from "@astrojs/image/components";
export interface Props {
  title: string;
  content?: string;
  image: string | any;
}
const {
  title,
  content = await Astro.slots.render("content"),
  image,
} = Astro.props;
---

<div class="items-left flex flex-col gap-4 pb-8">
  <Image {...image} class="w-2/3" />
  <div class="">
    <h1 class="text-xl font-medium text-primary">{title}</h1>
    <div class="mt-2 mb-4 h-0.5 w-full bg-secondary"></div>
    <p class="text-lg text-neutral">
      {content}
    </p>
  </div>
</div>
